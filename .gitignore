# Claude Code 로컬 진입점 (각자 로컬에서 `@AGENTS.md` 한 줄로 생성). 커밋 금지.
CLAUDE.md
CLAUDE.local.md

# 산출물 — 저장소 비대화 방지: 모든 깊이의 output/ 내용만 제외(폴더는 .gitkeep 유지).
# templates/ 마스터 pptx, research/ 근거 pdf 등은 정상 추적된다.
**/output/*
!**/output/.gitkeep

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / 에디터
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# LibreOffice 변환 임시 프로필
.~lock.*#
