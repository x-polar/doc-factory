# 문서 매니페스트 — 이 문서의 메타와 디자인 선택.
# deckbuilder가 읽어 브랜드 테마를 로드하고 오버라이드를 병합한다.

title: "<문서 제목>"
brand: _default        # brands/<brand> 선택. 폴더가 없으면 _default로 폴백.
version: v1            # 출력 파일명에 사용 (예: 20260629_<프로젝트>_v1.pptx)

# (선택) 이 문서만의 디자인 토큰 오버라이드 → 선택한 브랜드 theme.yaml에 deep-merge.
# 같은 브랜드인데 이 덱만 일부 색/폰트를 바꾸고 싶을 때만 사용.
theme: {}
#   colors:
#     accent: "0F9D58"
